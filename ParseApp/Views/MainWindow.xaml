<Window x:Class="ParserApp.Views.MainWindow"
        x:Name="this"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:local="clr-namespace:ParserApp"
        xmlns:view="clr-namespace:ParserApp.Views"
        xmlns:vm="clr-namespace:ParserApp.ViewModel"
        xmlns:converter="clr-namespace:ParserApp.Converter"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Title="MainWindow" Height="350" Width="525" >

    <Window.Resources>
    </Window.Resources>

    <Window.ContextMenu>
        <ContextMenu x:Name="MyContextMenu">
            <MenuItem Header="{Binding Text, Converter={StaticResource ToStringConverter}}" />
            <MenuItem Header="神" IsEnabled="{Binding IsChecked, ElementName=Check}"/>
        </ContextMenu>
    </Window.ContextMenu>


    <Grid x:Name="Grid1">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <TextBox Grid.Row="0" HorizontalAlignment="Left"
                 Text="{Binding Text.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 d:Text="数字を入力して欲しいテキスト欄"/>
        <Label Grid.Row="0" HorizontalAlignment="Right"
               Style="{StaticResource ValidationStyle}"
               Content="{Binding Text.ErrorMessage, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
               d:Content="エラーメッセージ"/>

        <Button Grid.Row="1" Content="ボタン"
                Command="{Binding RunCommand}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="IsEnabled" Value="False"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsChecked, ElementName=Check}" Value="True" />
                                <Condition Binding="{Binding Text.HasErrors}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="IsEnabled" Value="True"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        <CheckBox x:Name="Check" Grid.Row="2" Content="ボタンを押せるかどうか"/>
    </Grid>
</Window>
